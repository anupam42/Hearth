{
  "name": "snorlax-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "watch": "tsc --watch",
    "serve": "node scripts/dev-server.js",
    "dev": "node scripts/dev-server.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
